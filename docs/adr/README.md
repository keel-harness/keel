# Architecture Decision Records

One ADR per significant decision (ground rule 7). Copy `0000-template.md` for new
records; keep the number aligned with `MASTER_SPEC.md` §10.1.

**Reserved numbering gap:** ADRs jump from `0009` to `0016`. Numbers **0010–0015
are intentionally reserved** for later-phase decisions and are absent until those
phases land — this is not a mistake:

| Status | ADRs |
|---|---|
| Drafted (Phase 0) | 0001–0009, 0016, 0017 |
| Reserved (later phases) | 0010 provenance design · 0011 declassification scoping · 0014 hybrid-retrieval scoring · 0015 decay-rejected |
| Accepted (2A R1 audit/policy format freeze) | **0013** OAP conformance — stay an OAP-mappable superset, do **not** conform (single-vendor draft; OAP's record has no chain/seq/Merkle; resolves OQ-8) · **0056** capability manifest — one source of truth generating/validating policy ⇄ sandbox ⇄ egress ⇄ conformance tests (`policy_sandbox_mismatch` runtime finding as an open-payload marker, no new event type). Pairs with the ADR-0024 multi-axis revision + the `@keel/shared` `SideEffect` schema + §7 corpus (`docs/design/2026-06-22-side-effect-taxonomy-decisions.md`) |
| Accepted (Phase 0 hardening) | 0012 protocol versioning (forward-tolerant ErrorCode, SessionId/Sha256 fields, WARDEN_NOTIFICATIONS registry, JSON-safe wire fields) |
| Accepted (operational) | 0018 esbuild security override (temporary — GHSA-gv7w-rqvm-qjhr) |
| Accepted (operational) | 0019 ModelPort pre-freeze refinements (tool-call linkage, abort, streaming/reasoning chunks) |
| Accepted (Phase 0 hardening) | 0020 coverage-gate design (per-file floor closes C4, per-package §6.1 floors, measured property harness, pinned fast-check seed + testTimeout) |
| Accepted (Phase 1) | 0021 ExecutorPort — kernel↔execution swap seam (LocalExecutor → WardenExecutor) |
| Accepted (Phase 1) | 0022 OQ-3/OQ-4 benchmark pins (model claude-sonnet-4-6, caps $25/$300, reference harness terminus-2 via harbor@v0.13.2) |
| Accepted (Phase 1) | 0023 Epic 1.2 tool dependencies (`@vscode/ripgrep` for search; no native dep for bash — `PipeShellSession`; detached process-group kill) |
| Accepted (Phase 1 — spec v1.4; R1 re-ratified for 2A format freeze) | 0024 tool side-effect taxonomy (static capability + dynamic resolved effect; feeds policy/audit/retry; **RE-OPENED 2026-06-22 → multi-axis + composition/dataflow revision, re-ratified at the 2A R1 format freeze**) · 0025 context lifecycle & compaction architecture (§4.7 — layers, retention classes, typed `TaskState`, validation + probes, provenance-through-compaction seam) |
| Accepted (spec v1.4 — governs Phase 2; sequencing tightened by MASTER_SPEC v1.18) | 0027 Phase 2A/2B split — minimum trust plane (hash-chained audit, sandbox, egress, demo) vs evidence hardening (Ed25519, Merkle, offline verifier, redacted bundles); **audit format frozen at the 2A boundary**; original 2B-parallelism allowance is not used in the current build; developer preview now follows P2A + P2B + Epic 2.19 closeout/readiness unless the owner explicitly revises the sequence |
| Accepted (spec v1.5 — memory) | 0029 file-native topic-document memory vault — **entries-as-addressable-blocks-within-topic-docs**; per-block stable IDs + provenance/evidence; `index.md` generated TOC; no `memory-log.jsonl` in v1; evidence refs bind to ADR-0025; categories `project_fact·procedural·decision·environment_quirk·flaky_test·security_policy·preference·other` |
| Accepted (Phase 1 — Epic 1.3) | 0028 retry-policy refinement — transport retry (SDK `maxRetries`, classified-transient 408/409/429/5xx + Retry-After, never 4xx) is **not** a tool retry; mid-stream errors surface for model-driven recovery; read-only-idempotent tool carve-out + audit-visible retry events are Phase 2; **amends ADR-0016** |
| Accepted (Phase 1 — Epic 1.3) | 0030 provider-adapter capability table + temperature-under-reasoning correction — adapters **omit `temperature`** when `reasoningEffort` is set (current reasoning models 400 on any non-default value); the per-provider capability table is the home for reasoning/caching/native-tool divergence; **amends ADR-0019** |
| Accepted (Phase 1 — Epic 1.3) | 0031 full-fidelity recording format — keel-owned `Recording` zod schema (`@keel/shared`) built from the frozen `ModelStreamChunk` vocabulary (the exact chunk stream + per-chunk timings; usage is in the finish chunk; provider metadata is below the port boundary, out of v1); `RecordingModelPort` (capture) / `RecordedModelPort` (byte-for-byte replay); consumers = Phase-2 warden calibration, the ≥10-session propagation gate, the trajectory store; Phase-0 simulator script-reconstruction stub stays |
| Accepted (Phase 1 — Epic 1.3) | 0032 Epic 1.3 provider dependencies — `ai@6.0.197` + `@ai-sdk/{anthropic@3.0.81,openai@3.0.68,google@3.0.80,openai-compatible@2.0.48}`, all Apache-2.0, exact-pinned, `ignore-scripts`; pinned to the newest **≥7-day-old** `ai@6`-generation versions (latest were <7d old); local/Ollama via `@ai-sdk/openai-compatible` (`ollama-ai-provider-v2` evaluated, not needed for the 4-provider scope) |
| Accepted (spec v1.6 — DX/autonomy) | 0033 autonomy modes & approval UX (§4.9) — Guided/Autopilot/Project-Autopilot/YOLO as **policy postures over the warden**, not model behavior; **Autopilot ≠ YOLO** (high autonomy inside enforced boundaries vs. reduced enforcement); auto-proceed keyed off the §4.8 side-effect taxonomy + warden verdicts + grants, never model confidence; ledger/audit-derived receipt; scoped approvals + review queue; scope budget / broad-rewrite guard / low-confidence stop as **alignment** heuristics, not security; reuses the existing `mode.change` audit event + `once`/`project` grant scopes (**no schema/protocol change**); **extends ADR-0017's "may not"** (model may not set/raise its own mode); honest phasing — kernel surfaces P1, postures P2A, teach-from-corrections P3; **resolves OQ-13** (Guided default · Autopilot opt-in for trusted repos · default scope budget `medium` · `session` approval scope kernel-side over `once`, audited, no Appendix-A change) |
| Accepted (spec v1.7 — DX/steering) | 0034 mid-run steering & input queue (§4.10) — the **queued / interrupt / urgent** input classes (type mid-run without waiting or hard-interrupting); safe injection boundaries; mid-run input is a **session-ledger event** (ADR-0008) with reserved `input_id`/`class`/`inserted_at`/`changed_task_state`/`invalidated_plan` fields — **no frozen-schema change**; persisted, survives resume, preserved through compaction as recent-verbatim/constraints; Autopilot honors steering before scope expansion/mutation (narrowing eager · expanding via the §4.9.3 review path, ADR-0017); warden-audit-chain inclusion is a reserved Phase-2A-freeze seam (ADR-0027); kernel surfaces P1 (Epics 1.4/1.5/1.6) |
| Accepted (Phase 1 — Epic 1.4) | 0035 session ledger event model & crash-safety — persistence **folds the loop's public KernelEvent stream** (no loop change; round-trip property guards fidelity); refines the non-frozen `SessionEvent` union (assistant.toolCalls · tool_result `{name, output:string}` · removed standalone tool_call · `session_meta`/`run_status`/`steering` metadata); **held-fd writer + fsync + tolerant-final-line reader** (`SessionCorruptError` on non-final corruption; truncation property); **copy-prefix branch** + lineage; **StopReason relocated kernel→shared**; steering camelCase mapping of §4.10's reserved names; **rebuild synthesizes interrupted tool_results** for aborted/orphaned tool calls (resolves ER-012); ledger is keel-internal & agent-writable (not the audit chain), unredacted in P1 (DOC-LIMIT) |
| Accepted (Phase 1 — Epic 1.5) | 0036 TUI architecture — `UIPort` kernel↔renderer seam (kernel never imports Ink); a **pure reducer** is the single source of "what to show" + **dumb headless/Ink renderers**; **coverage-exemption boundary** (`tui/ink/**` exempt + snapshot-tested, everything else per-file ≥90, shared 100%); the **honest-posture HUD** (§4.9.1 — `●` only when enforced, Phase 1 shows `○ … no enforcement`, never a trust-mode word, golden-tested); mid-run steering applies via **re-drive** (no loop change — abort at a tool/turn boundary + `applySteering` + rebuild; `rebuild` dedups steering by `inputId` last-wins); the InputBar→runner bridge is a **single shared async iterator** (`InputQueue`, no demux race); **no new security claim** (renders the existing posture); runner takes injected ports — production construction + multi-turn REPL = Epic 1.6 |
| Accepted (Phase 1 — Epic 1.5) | 0037 TUI dependencies — `ink@7.0.5` + `react@19.2.7` (runtime), `ink-testing-library@4.0.0` + `@types/react@19.2.16` (dev), all **MIT**, exact-pinned, `ignore-scripts`, ≥7-day; **deviation from ADR-0003's pty plan** — test with **ink-testing-library (pure JS)** not **node-pty** (a native module whose install build fights mandatory `ignore-scripts`); real-terminal resize/escape verified manually + at packaging (Epic 1.10); **no markdown/syntax-highlight dependency** in Phase 1 (in-tree diff + plain markdown; rich rendering is a fast-follow behind its own gate) |
| Accepted (spec v1.5 — Epic 1.7) | 0026 declarative-only extensibility in the alpha — config/skills(SKILL.md)/AGENTS.md are **data, never code**; a skill is metadata + a markdown body loaded lazily as **inert text** (no code path executes it); **no in-process code loading in Phase 1** (the unsandboxed, key-holding kernel must not run project TypeScript before the warden); **executable/code-loading extensions resequenced behind the warden's `execute` gate** (post-Phase-2, same projection seam as governed MCP). Written when SKILL.md landed (was a §10.1 seed) |
| Accepted (Phase 1 — Epic 1.7) | 0038 workspace trust gate (trust-before-parse, §3.2(4)/SEC-012) — a **single trust-gated fs chokepoint** (`ProjectReader`) is the only project-metadata read surface; until trusted, every read returns empty + **zero real fs access** (the access log is the SEC-012 instrument). Decision resolved **before the loop**: explicit opt-in (`--trust`/`KEEL_TRUST`) → persisted → interactive prompt (TTY) → **fail closed to untrusted**; persisted **user-scope** (`<keelHome>/trust.json`, realpath-keyed, atomic, zod-validated, fail-closed), **never project-file scope**, **no tool writes it** (enforces ADR-0017's "may not mark trusted"). **In-process kernel-DX invariant, not the warden/containment** (Phase 1 honest-no-enforcement); closes the env-snapshot pre-trust gap. Honest limit: post-*decline* typed-tool reach is ungoverned in P1 (warden = Phase 2, ER-025) |

| Accepted (Phase 1 — Epic 1.9) | 0039 secrets handling — the **redaction filter** at the single `SessionStore.append` chokepoint (provider-key format catalog + a conservative entropy heuristic with ULID/uuid/sha false-positive guards; honest "known formats, not all secrets"; SEC-014 session-JSONL Proven, audit reuses it Phase 2A); a **`SecretStore` port** with a **pure-TypeScript `0600`-file store** as the v1 backend (resolve store→env; empty value never masks env) + `keel auth` (stdin, no echo, never prints the secret); the **config-dir guard** (`Workspace` `deniedRoots`, strict-descendant, both resolve paths incl. `--followSymlink`). **Decided AGAINST a native keychain binary in the credential path for v1** — `@napi-rs/keyring` passed the mechanical gate (MIT/≥7-day/no-install-scripts/prebuilt) but an opaque third-party native binary handling keys is what a security audit flags + marginal over `0600` for keel's single-user threat model (§3.3); the OS keychain is a vetted future adapter behind the port (preferring the OS's own tools). Reverses an initial native-binding attempt after the end-of-epic security review |

| **Accepted (Phase 2A / Epic 2.11)** | **0058** lifecycle manifest + validation posture — `.keel/lifecycle.yaml` is trusted repo validation intent, not authority; the kernel loads it only through `ProjectReader`, advertises `lifecycle.run` only for trusted valid manifests, and the warden resolves action argv from its own loaded manifest before lowering to the governed-bash `warden.execute` path. Lifecycle markers ride open audit payloads; validation posture extends ADR-0033 as policy-pack/run-profile data, not a second verdict engine. No Appendix A/B/D/E, frozen schema, all-tool, real-model product-path, receipt/status, compliance, or provenance-taint claim change. |
| **Accepted (durable learning and evidence UX)** | **0059** reviewed durable learning + evidence-derived receipts — proposed skills/memory remain reviewable, provenance-carrying diffs with no silent self-modification; end-of-run receipts and replay surfaces derive only from controller-owned session/audit evidence, preserve missing or unverified states, and pass redaction. No new authority, security claim, or frozen-format change. |
| **Accepted (run-control; Epic 2.12 product path shipped)** | **0060** /goal and /loop — audited run-control primitives (first-class `Goal`/`Loop` objects in `@keel/shared`, not prompt text; **persistence, not authority**). **/goal** completion adjudicated **structurally** — the completion audit generalizes the execution-grounded verify gate (`classifyCompletion()`); warden/ledger owns the verdict, never model self-report; `unverified` reported honestly; validation reuses Epic 2.11 lifecycle/posture (ADR-0058). **/loop** bounded **in-session only** — structurally-checkable exit (an executor-owned bash check on the governed session path), ADR-0044-style bounds, loop-detector progress **stop**, effect envelope **narrows only**; **NOT a scheduler/daemon** (scheduled loops → Phase 5+). No new authority (ADR-0017); no Appendix A/B/D change (ledger events + open `payload` + cited execution evidence, per ADR-0033/0034). Epic 2.12 has shipped the shared schemas, session-ledger metadata events, kernel ledger-based completion evaluator, public `keel run -p` and idle slash constructors, and a bounded in-session loop executor. Lifecycle-action loop checks and effect envelopes fail closed until warden profile narrowing is available; TUI HUD/receipt polish remains future UX work. Anchor: `docs/design/2026-06-24-goal-and-loop-run-control-spike.md`. |
| **Accepted (eval measurement; Epic 2.31a amended)** | **0062** bind eval runs and scoreboard entries to their claimed subset — pinned subset counts plus `assertRunMatchesSubset`; canonical `keel-tb2-full-89`; claim-grade `ScoreboardEntry.runEvidence` with commit/build/provider/profile/cache/budget/compaction/wall-clock/task membership; record-time guards for per-run aggregate math, per-run task membership, `aggregateQuality.nTrajectories`, cache/compaction consistency, and aggregate-only public JSON. |
| **Accepted (model-call governance; Epic 2.13 local product path shipped)** | **0065** governed model routing — local `ModelGateway` above frozen `ModelPort`: policy filter before preference, deterministic static routing before learned routing, fallback cannot cross policy/data boundaries, session route metadata, and `/model why` explains structured decisions. No Appendix A/B/D/E, `WARDEN_METHODS`, `AuditRecord`, `PolicyInput`, `SideEffect`, grant-scope, or `ModelPort` change; claim-grade model-call audit events require a separate ADR-gated schema bump. |
| **Accepted (Phase 2A / Epic 2.10)** | **0066** secretless egress credential proxy — trusted project config is loaded only after workspace trust; warden resolves `{env|file|command}` sources parent-side for governed `bash`; vendored SRT performs host-memory swap-on-access and placeholder Authorization substitution; wrong-host/unknown placeholder receives 403, existing Authorization is not clobbered, source files are deny-read, and missing sources deny + audit before execution. No Appendix A/B/D/E, frozen schema, `WARDEN_METHODS`, `AuditRecord`, `PolicyInput`, `SideEffect`, grant-scope, status/HUD, all-tool, real-model, signed/offline, or provenance-taint claim change. |
| **Accepted for Epic 2.26 local-stdio slice only** | **0067** governed MCP projection seam — pull MCP forward as a scoped tools-only local-stdio track before Phase 3: explicit review before spawn/connect, pinned definitions, namespaced tool projection, opaque conservative side effects, sandboxed server execution, POL-012-MCP sensitivity review, inert resource links, and open audit payload markers. Remote MCP remains gated on Epic 3.1 provenance enforcement; no frozen `WARDEN_METHODS`/audit/policy/schema change is assumed without a separate ADR. |
| **Accepted for Epic 2.34 implementation (active; adapter/dependency gates remain)** | **0069** interactive terminal console warden mediation — model-facing console operations (`open`, `send_keys`, `read_screen`, `release`, `close`) must route through the warden as target-scoped session grants plus per-operation policy/audit, with sandbox/egress containment, pre-return redaction/normalization, untrusted provenance tags, conservative opaque side effects, and no `node-pty`/native backend without separate dependency approval. Slice 8A implements a dependency-free system `tmux` private-socket command broker boundary with doctor-gated unavailable honesty, Slice 11 adds disabled-by-default parent-env product config wiring, and later local slices add typed QEMU launch config plus default-denied profile-gated release for external graders. Installed-adapter proof, qemu parity, and guest-governance claims remain open until current-head implementation and reruns prove them; broker-confirmed released VMs are explicitly no longer warden-controlled. |
| **Accepted for implementation** | **0070** headless-reviewed interactive console grants — a one-use, session-bound, warden-generated grant envelope for headless console opens. It preserves ADR-0069's target/profile/sandbox/policy grant material instead of target-name preapproval, keeps product config separate from authority, and requires human/parent review plus live warden revalidation before `interactive_console.open`. The maintainer approved the path on 2026-07-10 and delegated implementation choices to Codex under the existing no-corners-cut TDD/security/QC gates. |
| **Accepted (Epic 3.8 TUI security remediation; amended 2026-07-17)** | **0073** UIPort presentation evolution and live approval scope — freezes the three-method `UIPort` transport while permitting additive ADR-0036 presentation types; keeps approval state process-local and non-replayable; limits live review to once/session/deny and keeps durable project grants in explicit Project Autopilot configuration; rejects forged live project scope in the executor; treats post-submit transport loss as indeterminate; requires authorization audit plus persistence before project-grant execution; keeps Ink a dumb map over covered reducer/projection logic. No frozen RPC/audit/session/grant-schema change. |
| **Proposed (stop-and-ask)** | **0074** detached / long-lived service process lifecycle — code-verified that a backgrounded service (`nohup server &`) cannot outlive a governed command by construction on Linux (bwrap `--unshare-pid` PID namespace + `--die-with-parent`) and lingers uncontrolled on macOS (seatbelt, policy-only); governed bash already rejects the `lease` argument. Rejects honoring detach idioms in governed bash (Option 3); keeps governed bash lease-free and per-command-sandboxed (containment feature, not a bug); proposes documenting the same-command / interactive-console pattern and reaping the macOS subtree on normal completion for Linux parity (a *tightening*); defers a warden-mediated service-lease primitive (Option 2) to a real product need. Keeps the eval-only auto-lease. No new security claim; no frozen contract touched. |
| **Accepted (Epic 3.8e sandbox compatibility)** | **0075** warden-owned sandbox temporary root — each production warden creates one fresh canonical owner-only temp root, projects only that exact root as `declared_temp`, points sandboxed children at it, and removes it after child settlement without following replacement symlinks. Inherited temp paths, ambient `/tmp`, the workspace, and `KEEL_HOME` never select the authority root; failure is closed. No RPC/audit/session/grant schema change. |
| **Accepted (Epic 3.8e bounded control)** | **0076** terminal review mode and abandoned-review settlement — additive process-local `ExecutorPort` execution context forbids opening a new live human-review prompt after the turn controller disconnects while preserving policy/sandbox/audit and existing exact-grant resolution. A remaining or abandoned review is explicitly settled denied, returns terminal not-executed evidence, and persists as `blocked` with no review pending, so `/loop` stops without hanging or spending another model turn. No serialized contract or approval-authority widening. |
| **Accepted for prerequisites (Epic 3.10 Slice 2A; owner decision 2026-07-22)** | **0078** warden-observed mutation review — downgrades the portable producer from an unprovable atomic-operation diff to `observed-before → verified-installed-after` with `not-atomic` / concurrent-mutation-not-excluded scope; approves capability-negotiated `warden.presentation.take` under protocol `1.1.0`, an outer transport finalizer, module-private lazy resolver/occurrence associations, scheduler-bounded cooperative construction, and summary-only headless output; keeps raw images and structured presentation out of model/session/audit/eval serialization. SEC-310-01 landed independently first. The protocol-1.1 Appendix A / `PROTOCOL_VERSION` / ADR-0012 amendment is atomic and gated by 1.0-peer byte compatibility plus resolver-isolation tests; capability advertisement and producer behavior remain Slice 2B work after independent review and exact-head CI. |
| **Accepted (Epic 3.10 Slice 4; delegated owner decision 2026-07-23)** | **0079** receipt recovery without unsafe automatic undo — the current producer does not retain an owned full preimage, prove a clean Git baseline, or exclude concurrent mutation, so v1 receipts emit bounded file evidence and qualified manual recovery guidance rather than `git restore`, removal, or automatic-undo claims. `ran` remains distinct from controller-verified checks and absent validation is explicit. Future automatic recovery requires a separately ADR-gated owned-preimage/checkpoint and concurrency/dirty-baseline design. No frozen interface, durable format, security claim, or enforcement changes. |
| **Accepted (Epic 3.10 Slice 6; delegated owner decision 2026-07-23)** | **0080** TUI runtime truth vocabulary and copy ownership — controller presentation distinguishes starting, governed, deliberately unenforced, unavailable/fail-closed, and unreported states without release-phase inference; trust modes require a governed posture. Cross-surface truth vocabulary lives in `tui/strings.ts`; local one-off copy stays beside covered planners. Amends ADR-0003/0036; additive UI state only, with no frozen wire/durable format, enforcement, or security-claim change. |
| **Accepted (Epic 3.10 Slice 8; delegated owner decision 2026-07-23)** | **0081** informed approval presentation sources — separates model-requested tool, Warden-reviewed effective target, fixed review reason, strictly parsed exact reusable resource, choice consequences, and controller lifecycle next step. Missing facts remain explicit; model prose never substitutes for Warden authority. Live choices remain once, validated exact-resource session, deny, and explain; project remains external. Additive process-local presentation only, with no frozen wire/durable format, authority, settlement, enforcement, or security-claim change. |
| **Accepted (Epic 3.10 Slice 10C; delegated owner decision 2026-07-24)** | **0082** process-specific npx entrypoints — keeps the public `./bin/keel.mjs` command while splitting a small early-paint launcher from private Kernel and Warden bundles. Packaged Kernel resolves only the exact sibling Warden with `process.execPath` and fails closed if absent; source/dist selection and compiled-binary self-dispatch remain. The union build graph preserves complete license evidence. No public CLI/frozen protocol/durable format, authority, dependency, security claim, budget, or binary-publication change. |
| **Accepted (Epic 3.11; owner decision 2026-07-27)** | **0083** production-mode renderer with host-env restoration — the packaged npx release carrier captures the host `NODE_ENV`, forces production before the Kernel graph loads, and restores the exact host state while stripping internal sentinels at user-affecting child-process boundaries. The npx-only Kernel TSX transform emits the production automatic JSX runtime while React/reconciler/scheduler remain external; Warden, standalone binary, dev/tsx, dependencies, frozen contracts, enforcement, authority, audit, security claims, and the RSS budget remain unchanged. |
| **Accepted (Epic 3.14; explicit owner decision 2026-07-28)** | **0084** exact once-only local MCP invocation review — a trusted and pinned, non-quarantined, non-secret local-stdio MCP call whose current policy verdict is unmodified `review` may open one Warden-owned generic live review. Approval is bound to the exact request/server/policy/provenance/effect material, creates no reusable/session/project/headless authority, revalidates before execution, and still passes the existing live tools-list pin gate before `tools/call`; secret-sensitive calls remain terminally non-approvable and now retain base deny while adding `POL-012-MCP`. Reuses frozen protocol 1.1 and open audit payloads; no schema/event/CLI/grant-scope change. |
| **Accepted (Epic 3.21; explicit owner decision 2026-07-30; version target amended 2026-08-02)** | **0085** public npm release authority and artifact flow — publish `keel-harness@0.1.1` only from exact green public `keel-harness/keel` through one protected `release.yml` OIDC trusted publisher limited to `npm stage publish`; pack the npm carrier once, bind the exact tarball to checksums, complete Syft-produced SPDX/CycloneDX evidence, GitHub attestations, a draft release, staged byte inspection, and separate maintainer 2FA approval. The prior `0.1.0` candidate was staged but never approved or made public and must be explicitly resolved before the new tag. No long-lived npm token, direct CI publish, automatic retry, alternate-source publication, or standalone-binary release is permitted; ADR-0040's binary licensing hold remains unchanged. |
| **Accepted (Epic 3.22; explicit owner decision 2026-08-01)** | **0086** warden-owned connect-time egress address guard — the `srt:vendored` TCP path resolves every destination in the warden immediately before a new connection, classifies every answer, rejects the whole set if any address is unsafe or not narrowly excepted, and pins the vetted set to SRT's final dial. Private-address exceptions are owner-managed exact workspace+host+CIDR+port authority, separate from hostname grants. No generic `SandboxPort`, provider API, UDP/QUIC, CONNECT/SNI-equivalence, or claim-promotion implication. |
| **Accepted (2026-08-04)** | **0087** controller-enforced final-answer contracts — explicit task-scoped word bounds; one controller-validated tools-disabled rewrite; a deterministic honest fallback; one primary answer with the redacted original locally inspectable; additive optional settlement metadata on existing non-frozen session events; and deterministic closure of an exactly tagged interrupted rewrite prompt. Provider token ceilings are only a secondary rewrite rail, never the completeness contract. No implicit prompt parsing, global cap, repeated tool, frozen `ModelPort`, Warden, RPC, audit, policy, sandbox, egress, grant, dependency, or security-claim change. |
| **Proposed (stop-and-ask; issue #145)** | **0088** progress-earned terminal-review recovery — retain one initial correction, then permit at most one additional task-scoped correction only after a successful post-recovery typed `edit` or `write`; hard-cap the task at two corrections and one refresh. Reads, searches, prose, bash, failures, siblings, and correction success do not earn credit. Every correction remains one fresh unchanged model-authored call through ordinary Warden enforcement; no policy, grant, command-rewrite, frozen-contract, or security-claim change. |

**Note on 0068:** the interactive-terminal placeholder referenced ADR-0068 for the
process-lease / auto-lease decision, but no `docs/adr/0068-*` file exists in this
checkout as of 2026-07-09. ADR-0069 intentionally avoids reusing that number so the
process-lease decision can be reconciled without collision.

`0004` (policy engine) — spike **done** (2026-06-22; rebuilt 2026-06-23): opa-wasm measured (p99
~93µs, built-in gap = none for keel's pack); decision provisional, pending a `bun --compile` CI smoke
test; regorus documented fallback. `0007` (embedding runtime) remains `proposed — spike pending`
(finalized at its Phase 3 spike).

Numbers **0018+** record operational/supply-chain decisions that arise during the build
and are not enumerated in `MASTER_SPEC.md` §10.1.
